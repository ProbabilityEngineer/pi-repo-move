{
  "name": "pi-move",
  "version": "0.1.0",
  "description": "Pi extension for moving the current repo and relocating its Pi session bucket",
  "type": "module",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "lint": "tsc --noEmit"
  },
  "keywords": ["pi", "pi-extension", "move", "repo"],
  "license": "MIT",
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.74.0"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "latest",
    "@types/node": "latest",
    "typescript": "latest"
  },
  "pi": {
    "extensions": ["./index.ts"]
  }
}
